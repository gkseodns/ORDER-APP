# Render 등 배포 시 빌드 단계에서 사용합니다.
# Render 대시보드 → Environment 에서 VITE_API_BASE_URL 추가 후 값을 아래처럼 설정하세요.

# 배포된 백엔드 API 주소 (끝에 /api 포함)
VITE_API_BASE_URL=https://order-app-backend-1c7a.onrender.com/api

# 로컬 개발 시에는 설정하지 않으면 기본값 http://localhost:3000/api 사용
